# PA Bot - Python Dependencies
#
# Install (CPU — works everywhere):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# Install (GPU — requires NVIDIA CUDA 12.1+):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

# Deep Learning (CPU by default; see install commands above for GPU)
torch>=2.0

# Data & Analysis
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Technical Analysis
ta>=0.11

# Async HTTP
aiohttp>=3.9

# Async SQLite
aiosqlite>=0.19

# Configuration
pyyaml>=6.0
python-dotenv>=1.0

# Optional: charting / visualization
matplotlib>=3.7
